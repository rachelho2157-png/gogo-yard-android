# GoGo Yard Android

在 Android Studio 中打开本目录，等待依赖同步完成，然后选择 **Build > Build APK(s)**。测试包输出在 `app/build/outputs/apk/debug/app-debug.apk`。

默认包名：`com.lawnpilot.mower`。应用通过安全 WebView 加载已发布的 GoGo Yard 服务。
